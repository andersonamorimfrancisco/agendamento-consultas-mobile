import normalizeAvailabilityFilter from "./normalizeAvailabilityFilter";
import filterAppointmentAvailability from "./filterAppointmentAvailability";
import normalizeHour from "./normalizeHour";
import normalizePatient from "./normalizePatient";

export default {
  normalizePatient,
  normalizeHour,
  normalizeAvailabilityFilter,
  filterAppointmentAvailability
};
