import React from "react";

import { Container, Title } from "./styles";

interface MainProps {}

const Main = ({}: MainProps): JSX.Element => (
  <Container>
    <Title>Maiin</Title>
  </Container>
);

export default Main;
