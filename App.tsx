import React from "react";

import { Container, Title } from "./styles";

interface AppProps {}

const App = ({}: AppProps): JSX.Element => (
  <Container>
    <Title>App</Title>
  </Container>
);

export default App;
